var React = require('react');

var Login = React.createClass({
  render: function () {
    return (
      <div>
        <p>This will be the log in page</p>
      </div>
    )
  }
})

module.exports = Login;
